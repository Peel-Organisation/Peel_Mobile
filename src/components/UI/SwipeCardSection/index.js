import React from 'react';
import { Section } from './styles';

const SwipeCardTopSection = ({ User }) => {
  return (
    <Section>
      {/* Contenu du composant */}
    </Section>
  );
};

export default SwipeCardTopSection;
